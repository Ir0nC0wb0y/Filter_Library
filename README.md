# Filter Library
A library of the filters I have used and tested, used in other projects. This is my first library, so if I'm missing context/syntax/files/structure/etc be nice.

Right now, the following filters are included:
- Exponential Filter
  - single point filter by weight


## Installation

Copy the library into your Arduino libraries folder.
